# Use Node.js 22 for this project
# Try to use a pre-built binary if available
use nix -p nodejs_22 -p yarn -p nodePackages.npm

# Alternative: if the above fails, you can manually run:
# nix-shell -p nodejs_22 -p yarn -p nodePackages.npm
# 
# Or try with --impure flag to use pre-built binaries:
# nix-shell --impure -p nodejs_22 -p yarn -p nodePackages.npm
